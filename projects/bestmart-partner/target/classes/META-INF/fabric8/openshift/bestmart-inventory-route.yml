---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    expose: "true"
    app: bestmart-inventory
    provider: fabric8
    version: 1.0.0-SNAPSHOT
    group: org.mycompany
  name: bestmart-inventory
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: bestmart-inventory
